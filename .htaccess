# Redirect all traffic to the public folder

<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is not for public folder
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect everything to public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]

</IfModule>
